import { AdminPlaceholder } from '@/components/admin/admin-placeholder'

export default function AdminShippingSettingsPage() {
  return <AdminPlaceholder title="Shipping Settings" note="Shipping configuration is coming soon (currently: free over $300, $250 min order)." />
}

import { AdminPlaceholder } from '@/components/admin/admin-placeholder'

export default function AdminBlogPage() {
  return <AdminPlaceholder title="Blog Posts" note="A blog CMS hasn’t been built yet (no posts schema exists)." />
}

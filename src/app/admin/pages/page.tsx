import { AdminPlaceholder } from '@/components/admin/admin-placeholder'

export default function AdminPagesPage() {
  return <AdminPlaceholder title="Pages" note="Static-page management hasn’t been built yet (no pages schema exists)." />
}
